import javax.swing.*;


public class fifthFrameLinear extends JFrame {
    private JPanel controls, quadpanel; // the 2 JPanels will include JSliders and JLabels
    private JSlider aslider, kslider, dslider, cslider; // the JSliders will let the user input values for a,k,d,c through sliders
    private JLabel alabel, klabel, dlabel, clabel; // these JLabels will show what value the slide is at\
    fifthFrameLinear() {
    }
}
